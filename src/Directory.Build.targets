<Project>

  <PropertyGroup Condition=" '$(SystemWebPublicSign)' == 'true' ">
    <PublicSign>True</PublicSign>
    <AssemblyOriginatorKeyFile>$(MSBuildThisFileDirectory)MSFT.snk</AssemblyOriginatorKeyFile>
  </PropertyGroup>
  <Import Project="..\Directory.Build.targets" />
</Project>
